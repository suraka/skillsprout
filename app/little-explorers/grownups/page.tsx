import type { Metadata } from 'next';
import { GrownupControls } from '@/components/little-explorers/pages';
export const metadata: Metadata = { title: 'Grownup controls · Little Explorers' };
export default function Page() { return <GrownupControls/>; }
