# Sorting Garden v1 — guest rules simulator

Entry `/demo/sorting-garden`: React block list → typed project validation → bounded interpreter → structured trace/semantic SVG stage. Academy provider is bypassed on this route; no auth/catalog/backend/AI/analytics calls, file upload, camera/mic or persistent guest storage.

Project `{schema_version:1, workspace_state:{blocks:Block[]}}`; four discriminated block types (start/sample/rule/show); unique bounded IDs; valid sample/rule enums; validated order; maximum 12 blocks, history 30 edits. No eval/generated code or ambient I/O in runtime. Server-rendered controls stay disabled until React can handle events, with a loading message and no-JavaScript alternative. Run timer advances 450ms/step; Pause cancels timer; Step one block; edits clear stale output; reset undoable; Finish clears state; Back-cache restore resets guest workspace.

Synthetic red-round fruit, green-pointed leaf and green-round fruit. Fixed color rule misclassifies green fruit; shape rule repairs this sample. Clearly labeled rules, never trained AI or calibrated confidence. Inspector uses actual execution trace. Native input/order controls are keyboard alternatives to optional dragging. No unsupported Save button. SVG paths/styles/copy are original, system font, no competitor assets.

Guest backend/database/auth components are explicitly NOT APPLICABLE per specification §§10/12/15. Phase 2 must add server owner/tenant, immutable project revisions, lesson links, private save/reopen, optimistic concurrency and authorized evidence together. No course schema or old progress rows are changed by this demo. Pedagogy/family/device review remains pending.
